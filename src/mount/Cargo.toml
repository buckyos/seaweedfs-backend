[package]
name = "dfs-mount"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
fuser = "0.15"
libc = "0.2"   
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }
md-5 = "0.10"
dfs-common = { path = "../common" }
